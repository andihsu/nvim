# AndiHsu's Personal Neovim Confiurations.

There are three versions of configurations.

v1 is using packer.nvim as packge manager

other two versions of configurations use lazy.nvim

(v1 is recommended.)
